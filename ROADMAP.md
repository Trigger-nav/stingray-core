# Stingray — Working Prototype Roadmap

**Version 0.1 · July 2026 · Target: sea-trial milestone in ~6 months with 2–4 engineers + naval-architecture consultancy.**

Goal: from the current demo (validated UX + optimiser shape, synthetic everything) to software running against a real vessel and real weather, producing advisory passages and voyage debriefs.

**Carries forward from the demo:** mission-weights model, vessel-state/chat UX, twin component structure, DP optimiser shape, ingest scripts.
**Gets replaced:** synthetic twin coefficients, hand-drawn coastlines/bathymetry, corridor-limited routing, deterministic chat, everything in-browser.

---

## Phase 0 — Bench (weeks 1–8)

*Exit criteria: optimiser produces plans on real forecasts over real geography; verification methodology signed off.*

| # | Task | Notes |
|---|---|---|
| 0.1 | Monorepo scaffold: core (Python or Rust), CI, tests | Decision: Python first for model velocity; port hot paths later |
| 0.2 | Port demo optimiser core with unit tests | Weights model, constraints, ETA window carry over 1:1 |
| 0.3 | Real geography: GSHHG coastline land mask + GEBCO bathymetry | Free, global; replaces hand-drawn polygons |
| 0.4 | Open graph search: time-expanded A*/DP over sea-space lattice | Corridor DP becomes the fallback/fast path |
| 0.5 | GRIB pipeline in production: NOMADS + ECMWF open data, cfgrib parsing | **Done and verified end-to-end (2026-07-07):** both fetchers run live against real endpoints; cross-source consistency check passed (wave direction 16° mean disagreement — WW3 from-convention empirically confirmed); cfgrib fixture tests pass. Scheduling deliberately out of scope (edge-first — belongs with the Phase 1+ edge device, not this bench milestone). Residual: tighten `_find_var` matching (real cfgrib names now known). |
| 0.6 | Twin v1 offline: fit/validate tooling for the parametric components | **Done (2026-07-08), built against synthetic data per the agreed approach — did not wait for the naval-arch consult.** New `fit/` package (depends on `core/` one-way + `scipy`, own extras group — see CLAUDE.md's package-layout note): steady-state segment extraction (rejects manoeuvring/transients/tank-transfer artefacts, `fit/segments.py`); sequential prior-regularised least-squares fit of calm resistance + SFOC (`fit/calm_resistance.py`) then added resistance (`fit/added_resistance.py`, calm/SFOC held fixed); priors as per-parameter distributions (`fit/priors.py`, every one **provisional with a real published-method source**, pending naval-arch review); holdout validation, not in-sample point estimates (`fit/validate.py`). **Acceptance test passes** (`tests/test_fit_acceptance.py`): synthetic telemetry from a known, deliberately off-prior `VesselSpec` with realistic input+output noise + injected junk → fitted predictions agree with ground truth within 15% across a held-out grid, junk correctly excluded. Also verifies a required review amendment: single-engine-config data (a real identifiability degeneracy between the power curve and SFOC, see CLAUDE.md's gotcha) degrades gracefully rather than producing a confident-but-meaningless fit. `python3 -m fit.cli --synthetic-demo` runs the whole thing standalone. Naval-arch review of the model forms + prior values is the still-pending follow-up — a later, short review alongside 0.7's draft, before the Phase 1 pilot fit (ticket 1.5), not a blocker to this ticket. |
| 0.7 | **Savings-verification methodology** (counterfactual baseline maths, written + reviewed) | **Drafted (v0.1, July 2026):** `docs/methodology/savings-verification.md` + branded Word export. Core construction: model-counterfactual baseline with same-passage bias correction (savings = difference of two twin evaluations under identical weather — correlated model error cancels); P10 reporting for contractual claims; full-denominator anti-gaming rules; Phase 1 crossover validation programme. Pending: naval-arch + counterparty review (open questions listed in the doc); countersigned version removes `baseline_provisional=True`. |
| 0.8 | Routing safety constraints: min depth, TSS, no-go polygons from chart data **+ fine lattice refinement (adaptive resolution) near tight constraints** | **Done (2026-07-08).** Minimum depth is a hard constraint (`core/legs.py`: vessel `draft_m` + `min_under_keel_clearance_m`, pruned like land/no-go), with a `DEPTH_EXEMPT_RADIUS_NM=1.5` pilotage exemption near declared origin/destination (real ports/anchorages are frequently shallower than open-water margins — captain/local-knowledge scope, not this optimiser's, with regression tests covering both a real shallow port approach and a real shallow anchorage). No-go zones (`core/geography.RealGeography`) now load real, cited zones (marineregions.org gazetteer, MRGIDs 26848/3457) from `data/geography/nogo_western_med.json` instead of synthetic placeholders. Bonifacio Strait TSS is modelled as a **separation zone only** (hard no-go, `data/geography/tss_western_med.json`) — directional lane compliance is explicitly out of scope this ticket (no free, verified source for the real IMO lane geometry found); the file is clearly labelled placeholder. Adaptive per-stage lattice refinement (`core/lattice.py`'s `build_lattice(..., geography=...)`) handles Bonifacio's genuine scattered-islet resolution need, refining down to a 0.5nm floor with multi-pass diagnostics when still degraded at the limit. **This, combined with fixing a side-diversity-filter bug** (was constraining lane sign passage-wide instead of just the Corsica-spanning region, silently forcing the west route onto real Sardinian coastline at the final approach) **restores a feasible west-of-Corsica strait transit** — both regression tests flipped from diagnosing the strait as unreachable to asserting it's reachable (`tests/test_optimiser_regression.py`), and the charter-window regression now finds the tighter window feasible via the west route. See `CLAUDE.md`'s Bonifacio gotcha for the full empirical trace and `docs/plans/ticket-0.8.md` for the real-data sourcing record. Follow-ups: real TSS lane geometry (chart/IMO routeing-system publication), and precise no-go boundaries — Scandola/Iles Lavezzi are real, cited rectangular bounding boxes (marineregions.org), not the legal designation polygons themselves (each zone's `precise_boundary_verified` flag in the data file). |

## Phase 0→1 bridge — the live planner (agreed July 2026)

With Phase 0 complete, the core has no user-facing surface: the hosted demo still runs its synthetic JS brain. The bridge closes that gap and starts Phase 1's non-code tracks, **with a feature freeze on the optimiser until ticket 1.5 (real-data twin validation) renders its verdict** — every new feature before then builds on unvalidated foundations (risk #4).

| # | Task | Notes |
|---|---|---|
| B1 | API service: FastAPI wrapper over `core.optimiser.optimise` + scheduled GRIB fetch | **Done (2026-07-09) — expanded scope, folding in B5's amended MVP shape per direct instruction.** Delivers both services B5 describes, packaged as one cross-platform installer: **`api/`**, a job-shaped planner service (submit → job id → poll, `concurrent.futures.ProcessPoolExecutor`, no Celery/Redis) wrapping `optimise()` unmodified, with versioned pydantic mirrors of `PlanRequest`/`Candidate`/`VesselSpec`/etc. and a CI-enforced drift test against the real `core/` dataclasses (`tests/test_api_schema_parity.py`); and **`capture/`**, the logging service — an NMEA 2000 gateway abstraction (`YachtDevicesEthernetGateway`/`ActisenseSerialGateway`/`ReplayGateway`) with real, cited-and-verified protocol framing (canboat's reference implementation for Actisense; a real, cross-checked example dump for Yacht Devices RAW), PGN decode via the `nmea2000` PyPI package, and a canonical tier-flagged telemetry schema with SQLite storage (WAL + busy_timeout, verified under genuine cross-process concurrent read/write). One `STINGRAY_ROLE=cloud\|vessel` flag makes the identical codebase serve both deployment targets (contract point 2): cloud role runs the existing, unmodified `ingest.fetch_grib_*` on a cron and serves the compact npz; vessel role never touches `ingest`/eccodes at all and instead opportunistically pulls that npz over conditional HTTP, feeding the same hot-swap mechanism cron does — which is also what keeps the Windows/macOS PyInstaller build free of eccodes' notoriously fragile cross-platform packaging. Packaging itself was real-verified this ticket, not assumed: a genuine PyInstaller trial build (macOS/arm64) surfaced and fixed two empirical gaps (`nmea2000`'s dynamically-imported `decoder_formats`/`encoder_formats` need explicit `hiddenimports`; `ProcessPoolExecutor` workers re-exec the frozen binary and need `multiprocessing.freeze_support()` before CLI-argument dispatch or the pool comes up broken) and confirmed the built binary serves real HTTP requests and runs a real `optimise()` job end-to-end. Windows service wrapper: NSSM, not `pywin32` (decided during that trial build — zero Windows-specific code needed once `freeze_support()` is in place). Deliberately out of scope, stated not hidden: real-vessel PGN-variability handling (ticket 1.3's job), IMU hardware, multi-tenant auth (ticket 1.4), and a real Windows/Linux CI-runner build (only the local macOS trial has actually run — see `deploy/README.md`'s checklist). See `docs/plans/ticket-B1.md` for the full design. |
| B2 | Connect the demo UI to the API: swap the `<script id="core">` block for API calls | **Done (2026-07-09).** `prototype/stingray_planner.html`'s synthetic `<script id="core">` brain (SCENARIOS/TWIN/corridor DP) replaced with a job-shaped `optimise()` client (submit → poll `/v1/plans`, 1.5s interval, 90s deadline) plus a field-mapping shim (`mapCandidate`) keeping ~450 lines of render/chat code unchanged. `render()` split into `requestPlan()` (network, debounced 350ms on sliders, undebounced on ETA-window/presets) and `renderUI()` (pure redraw, used by scrub/plan-click/tab-switch — no new job), both stale-response-guarded by monotonic generation counters. Fuel display switched to real kg (was synthetic litres); CO2 recomputed from `GET /v1/vessel`'s `co2_per_kg_fuel`. The synthetic weather-scenario dropdown is gone, replaced by a real provenance line from `GET /v1/health`. Vessel-state panel's "optimality" comparisons now use only `candidates`+`baseline` (2-4 points, not the old ~16-candidate internal sweep) — an accepted precision loss, not a bug (`core/optimiser.py`'s return shape is untouched, keeping this outside the bridge-section optimiser feature freeze). **Amendment caught in review:** a third, initially-missed `SCENARIOS` call site (`drawWx()`, the chart's weather heatmap/wind layer) is now served by a new, small, read-only `GET /v1/weather/field?h=N` endpoint (`api/weather_field.py` — downsampled 30×32 grid, hour-quantized, ETag-cached; built entirely on `core.weather.WeatherField.sample()`, no `core/` changes). Also added in the same amendment: a queue-depth cap (`max_queue_depth`, default 20) returning `429`+`Retry-After` on `POST /v1/plans` (`api/jobs.py`), protecting the bounded worker pool from unbounded submission backlogs; CORS middleware (`api/main.py`, `allow_credentials=False` — Basic Auth via header, not cookie) with `STINGRAY_CORS_ORIGINS`; and a documented mixed-content requirement (`prototype/deploy/HOSTING.md`) — the deployed demo is HTTPS (GitHub Pages), so `API_BASE` must be an HTTPS cloud URL or the browser silently blocks every request. Deploy workflow's 6-hourly weather-refresh cron and `livewx.js`/`fetch_weather.py` are deleted outright — the demo now sources weather live from the API, not a build-time static snapshot. See `docs/plans/ticket-B2.md` for the full design and the approved amendment. **Manual real-browser verification passed (2026-07-09)** — a real local API + UI session (prototype repo) surfaced and fixed three real bugs, none present in `pytest`/`ruff`: `baseline.score` is `NaN` by design (baselines aren't scored) but `vstateRows()`/`renderVessel()` were feeding it straight into `Math.min`/`.find()` comparisons, which either poisons the result or throws outright (`.find()` returning `undefined` on an all-`NaN`-losing comparison) — both now filter to `Number.isFinite(c.score)` before comparing; and a `renderUI()` crash (unrelated JS bug, not a network fault) was surfacing to the user as "CONNECTION FAILED" via `requestPlan()`'s catch-all — non-`ApiError` failures now get their own honest `ui_bug` classification instead of masquerading as a connectivity problem. Also fixed as a doc-consistency follow-up: `api/main.py` had no `if __name__ == "__main__":` block despite `deploy/stingray_cli.py`'s own docstring promising `python3 -m api.main` as "the normal, non-packaged dev invocation" — added, and `deploy/README.md` gained a "Local dev server" section (incl. an `lsof`-based orphaned-process troubleshooting note, written from a real instance of exactly that happening during this verification pass). Verification also surfaced a real chat-flow gap (fixed, prototype repo `159a801`): the "arrive by" chat path triggers a real 20-60s replan with no acknowledgment, reasonably reading as dead — now pushes an immediate "replanning — this takes up to a minute…" chat message before awaiting it. See the prototype repo's `27a8c7e`/`159a801` for the UI fixes. **A fourth, more substantial bug also surfaced**: a `latest_arrival_h`-constrained replan against `RealGeography`+real weather could exceed the UI's 90s poll deadline even though unconstrained requests ran in ~40s on the same machine. Root cause, confirmed by profiling: `core.optimiser.optimise()` was running its isochrone pre-pass (`core.isochrone.reachable_within`, `optimise()`'s dominant cost) *twice* whenever the request's window was tighter than the 48h default — once for the request's own horizon, once again for the baseline's. Fixed at the root (`core/isochrone.py`/`core/optimiser.py`): one shared Dijkstra pass, both horizons derived as cheap post-filters — provably equivalent, not an approximation (see `arrival_times_within`'s docstring); re-profiled constrained and unconstrained to identical cost after the fix. `tests/test_optimiser_regression.py` gained a regression test asserting the search runs exactly once. As defense-in-depth against worker-pool contention under real concurrent use (a separate, real risk the server-side fix doesn't address — the client never cancels superseded-but-still-running jobs), the UI's poll loop no longer hard-fails at 90s: that's now a soft deadline ("still computing…" instead of giving up) with a 5-minute hard ceiling, so a slow-but-still-progressing job isn't abandoned mid-flight. Full details: CLAUDE.md's gotcha. |
| B3 | Pilot recruitment pack: one-pager, data-agreement outline, install survey | `docs/pilot/` — feeds ticket 1.1 |
| B4 | Naval-arch engagement: single short review covering 0.6 model forms/priors + 0.7 methodology | Both artefacts ready; engage before Phase 1 fit |
| B5 | Capture kit BOM + order (ticket 1.2 hardware) | **Amended (July 2026): MVP deployment target is the yacht's bridge PC** — planner service + logging service installed locally (single installer; core is pure Python, cross-platform), with an **NMEA 2000 gateway as the entire hardware BOM — preferred: Yacht Devices YDEN-02 Ethernet gateway** (bus-powered, no host drivers, serves any listener on the ship LAN — logging isn't hostage to one PC or USB port), **with Actisense USB (NGT-1/NGX-1) supported as the alternative** for vessels where LAN access at the backbone isn't practical. Ingestion layer must abstract over both (Ethernet TCP/UDP stream vs serial-over-USB). **IMU: optional early add, not committed** — the ingestion/telemetry schema should accept a motion source if present (tier-flagged), but nothing in MVP scope depends on it. Heavy weather ingest stays cloud-side (vessel syncs compact npz; planning stays offline-capable on last-synced weather). Known deferrals, stated not hidden: no IMU → comfort model stays priors-only until Phase 2; always-on logging depends on the PC staying up — sleep/reboot gaps are the main data risk (survey + commissioning checklist item). Dedicated edge box (IMU, isolation, always-on by construction) remains the full-tier product, Phase 2. **Software side delivered by ticket B1 (2026-07-09)** — the gateway abstraction, telemetry schema, and cross-platform installer described above are built; this row's remaining scope is the physical BOM/order itself (a real YDEN-02 and/or Actisense unit for the manual hardware-verification steps `deploy/README.md` still lists as pending). |
| B6 | **Historical-data acquisition** — season(s) of monitoring-system exports, e-logbook dumps, noon reports, bunker records from candidate vessels/fleets | Potentially the fastest route to ticket 1.5's verdict: real-data twin fits *before* any hardware goes aboard, and it pre-builds 0.7's B2 historical baseline. A management company can provide fleet history under one NDA. Limits (don't oversell): usually no wave/motion data, coarse fuel resolution — accelerates the cold start and validates fitting on real-world mess; doesn't replace the capture kit. Technical enabler: **ERA5 reanalysis hindcast ingest** (small variant of the 0.5 pipeline — reanalysis instead of forecasts) to reconstruct weather along historical tracks. Ask is already in `docs/pilot/install-survey.md`. |
| B7 | **Historical-fit readiness** (enables B6; the fit pipeline itself is already passage-agnostic — `fit/` never touches routing) | **Done (2026-07-14).** All four parts delivered, plus a step-0 foundation (`core/track.py`'s `TrackPoint`/`covering_bbox`/`covering_time_range_s`, the `WeatherSample` precedent for a shared cross-package data contract) that Parts 1–2 both depend on. (1) *R1-lite data layer*: `--bbox` added to all four ingest scripts (`fetch_gshhg.py`, `fetch_gebco.py`, `fetch_grib_ecmwf.py`, `fetch_grib_nomads.py`), with a clobber guard (differing bbox requires explicit `--out`) and a real gap fix found in the process — `RealGeography._check_in_bounds` now derives bounds from the *loaded instance's own grid*, not the module-level `OPERATING_AREA_BBOX` constant (`core/lattice.py` deliberately still uses the constant directly for lattice-clipping, untouched, routing freeze respected throughout). (2) *ERA5 track annotator* (`ingest/fetch_era5_track.py`, `cdsapi`): builds one in-memory `GriddedWeatherField` from a single CDS request per track and reuses its existing, unchanged `.sample()` — zero new interpolation code. **Two real defects found during live verification** (a real CDS account was already set up) and fixed: a combined wind+wave request returns a **zip archive** with wind/wave split into separate files, not one raw NetCDF; and ERA5's wave stream is natively **coarser** than its wind stream, requiring the same kind of resampling `fetch_grib_nomads.py` already does for GFS/WW3, with a broadcast fallback for the degenerate single-grid-cell case. Live cross-check against a real, already-committed ECMWF open-data sample (ticket 0.5) confirmed the `mwd` from-convention assumption (11° disagreement, same order as ticket 0.5's own 16° cross-source finding) — see `docs/plans/ticket-B7.md`'s "Live ERA5 verification result". (3) *Import layer*: `fit/import_schema.py` (`CanonicalImportRow`, `SOURCE_DEFAULT_FUEL_NOISE_STD_FRACTION`), four adapters (`fit/import_adapters.py`: monitoring CSV, e-logbook, noon report, annotated-track), and `fit/import_pipeline.py`'s `rows_to_segments` orchestration. **Correcting this row's own prior framing**: "per-source STW-vs-SOG handling via the existing input-noise terms" overstated what existed — no such mechanism existed before this ticket; `fit/import_pipeline.py`'s per-`(vessel_id, passage_id, source)`-batch `stamp_segment_provenance` (via `dataclasses.replace` post-extraction) is what actually threads per-source noise into `SteadyStateSegment.fuel_noise_multiplier`, found necessary during plan review (amendment 1) after the original draft's `TelemetrySample`-based path silently discarded all provenance. Simulator adapter for Plymouth University's format is a **named follow-up, not built** — format still unknown. (4) *Passage-level holdout*: `fit/validate.py`'s `passage_holdout_split` (groups whole passages/vessels, never splits one across train/holdout) plus `fit/pipeline.py`'s new `fit_twin_from_segments` entry point — found necessary during implementation since `fit_twin`'s own internal segment extraction always discards identity, so real B7 imports call `fit_twin_from_segments` directly on `rows_to_segments`'s already-stamped output. Empirical finding (not theorised in advance): the flat vs. grouped split's *mean* RMSE isn't reliably ordered for this repo's low-DOF physical fit, but the grouped split's run-to-run RMSE *variance* is far larger (~3x in the test) — a flat split looks falsely stable regardless of which real passage a deployment would face. A second review pass caught one more invented-numbers gap in the same function — `daily_rows_to_segments` was silently defaulting `mean_heading_deg`/`active_engines` to `0.0`/`1` when a daily row lacked them; fixed by adding both to the required-fields check (no fallback), plus a minor fix widening `rows_to_segments`' SOG-fallback noise band to fire on `any()` (not `all()`) STW-missing row in a batch. Full design, all judgment calls, and every review round recorded in `docs/plans/ticket-B7.md`. `pytest -m ""` (400 tests) green, `ruff check .` clean, zero diffs to `core/lattice.py`/`core/optimiser.py`/`core/corridors.py`. |

Production bridge app (TS/React, offline-capable — ticket 2.2) starts during Phase 1, maturing alongside the data the capture kits gather.

## Phase 1 — Real data (weeks 6–16, overlaps Phase 0)

*Exit criteria: 30+ sea-days of telemetry from ≥1 vessel; twin v1 prediction error quantified and acceptable (fuel ±10% per passage at NMEA-tier).*

| # | Task | Notes |
|---|---|---|
| 1.1 | Recruit 1–2 friendly pilot yachts (LOI, data agreement) | Yacht-management relationships start here too |
| 1.2 | Capture kit from off-the-shelf parts: industrial ARM box, Actisense/Yacht Devices NMEA 2000 gateway, USB IMU, LTE | No custom hardware until rev B; ~€2k/kit |
| 1.3 | Edge logger: bus ingestion, signal normalisation, local store, store-and-forward sync | First real encounter with PGN variability — budget time |
| 1.4 | Minimal cloud: ingest API, TimescaleDB, auth, one ops dashboard | Boring tech, single provider, IaC |
| 1.5 | Fit twin v1 to pilot-vessel data; publish error bands | **Make-or-break validation of the core bet** |
| 1.6 | Install-survey checklist + commissioning runbook | Feeds the one-day-install claim with evidence |

## Phase 2 — Closed loop (weeks 14–26)

*Exit criteria: sea-trial passage planned by Stingray; predicted vs actual within stated confidence; first voyage debrief produced.*

| # | Task | Notes |
|---|---|---|
| 2.1 | Twin online learning on the edge; confidence intervals surfaced end-to-end | Kalman bias states + residual model per architecture §4 |
| 2.2 | Planner web app (demo UX rebuilt properly), served from edge box, offline-capable | Vessel-state visual + plan cards + mission presets |
| 2.3 | ECDIS route export (RTZ) | Keeps navigation in navigation software |
| 2.4 | Chat v1: LLM grounded in plan/twin state; demo's deterministic answers become its tools | Guardrails: never invents numbers; every figure from the twin |
| 2.5 | Live re-optimisation underway (forecast drift + performance drift triggers) | Quiet-by-default thresholds |
| 2.6 | Voyage debrief report generator (plan vs actual vs counterfactual) | The compliance/SEEMP evidence artefact — also the sales artefact |
| 2.7 | **Sea trial** on pilot vessel; publish results internally | The "working prototype" milestone |

## Beyond Phase 2 — route coverage (single route → global)

Superyacht movements are concentrated: Med summer, Caribbean winter, one transatlantic repositioning corridor, plus a handful of growing regions. **~6 region packs + 1 ocean corridor cover ~90% of superyacht miles** — global coverage is staged expansion, not a rewrite.

| Stage | Scope | Nature of work |
|---|---|---|
| R1 — Parameterise the region | **Done (2026-07-14).** `core/regionpack.py`'s `RegionPack` (bbox, `ref_lat_deg`, geography/weather paths, named ports, pack-tunable lattice knobs, hand-drawn-corridor registry) is the checked-in expression of the engineering rule: `data/region_packs/med.yaml` holds the Med's own numbers as pack data, matched exactly by a zero-I/O `MED_PACK` Python constant `PlanRequest.region_pack` falls back to (a drift-guard test keeps the two from diverging — `core/` must never gain a working-directory-relative file dependency in a default). `core/lattice.py`/`core/corridors.py`/`core/legs.py`/`core/optimiser.py` now take `bbox`/`ref_lat_deg`/`lane_turn_rate_nm`/etc. as explicit, Med-defaulted parameters instead of hard-importing the old module constants; `pytest -m ""` passed **unmodified** throughout this threading pass — the ticket's own "zero behaviour change on the Med" discipline, replacing the routing feature freeze for exactly this ticket. Arbitrary-endpoint routing turned out to already be built (B6, ahead of this ticket) — the real gap closed here was the region-pack concept itself. Found and fixed along the way: `_route_signature`'s Corsica-fallback bug (unconditionally labelled every route "W" for any region west of 9°E — a labelling bug only, search correctness was already fine). Hand-drawn corridors are now demoted via `RegionPack.legacy_corridors` (empty for every pack but "med"), not an accident of non-matching defaults. `api/` gained a multi-pack `AppState` (dict-keyed geography/weather per pack, all three `_load_weather` call sites covered including the hot-swap watcher), `pack_id` on `PlanRequestIn`/`GET` weather endpoints (with a pack-aware ETag — an unqualified one could 304 a wrong-region response), and SQLite-backed per-vessel favourites (`api/favourites.py`, a separate file from `capture/`'s telemetry DB, confirmed inheriting ticket 1.4's multi-tenant auth debt rather than solving it). `ingest/fetch_wpi_ports.py` hits NGA's real, live World Port Index API (found by inspecting the site's own React bundle — no documented endpoint existed to just look up), confirming two real, previously-invisible port-pin issues in the acceptance run below. **Real second-pack proof**: a UK South-West / Channel Approaches pack (Plymouth–Falmouth, entirely outside `OPERATING_AREA_BBOX`), generated end-to-end via real GEBCO/GSHHG/NOMADS/WPI ingest, produces a real feasible `optimise()` plan (`tests/test_uk_sw_pack_acceptance.py`) — the Med's own tuned lattice knobs needed no re-tuning; two endpoint findings (WPI's raw port pins landing on rasterised land / a 0m-depth shoal) were fixed by nudging to a verified-navigable nearby approach point, not by inventing data. | Refactoring, not new science — but the routing surface (`core/lattice.py`/`core/corridors.py`/`core/optimiser.py`) was under the bridge-phase freeze, so the discipline that replaced it here was "zero behaviour change on the Med," mechanically checked (`pytest -m ""` green, unmodified, before touching `api/`). Full design, all judgment calls, and the real acceptance-run trace: `docs/plans/ticket-R1.md`; pack-generation steps for a future pack: `docs/region-pack-runbook.md`. |
| C1 — Real surface currents (regional) | **Done (2026-07-20).** Pulled forward from R3's "currents become a real field" line, standalone at *regional* scale (not R3's ocean-crossing/spherical-geometry work) — motivated by the UK South-West pack, where tidal streams routinely dominate CTV-class routing. CMEMS-sourced surface currents (`ingest/fetch_currents_cmems.py`, real dataset ids verified live against the product's own STAC metadata and a real `copernicusmarine.describe()` call, not guessed) merge onto the existing wind/wave npz (`ingest/merge_currents.py`, absolute-UTC-time intermediate schema so a differently-cadenced product's own cycle-relative axis merges unambiguously) into the `current_u_ms`/`current_v_ms` plumbing `core/weather.py` has carried since ticket B2 — only the ingest side ever wrote zeros. **Two real, freeze-compatible `core/` fixes found by tracing what "real current" actually exercises, previously unreachable at zero current**: (1) `core/units.py`'s current-triangle math raises when cross-current exceeds STW — real UK tidal streams can trip this, and nothing caught it before, so one infeasible (speed, leg, current) combination used to fail an *entire* plan request instead of being pruned like every other hard constraint (`LegResult.current_exceeds_stw`, threaded through all three search call sites); (2) current sampling switched from unmasked `bilinear` to `bilinear_masked` (`core/weather.py`) — a real ocean-current product masks land far more aggressively near shore than wind does, exactly where routing endpoints live. `pytest -m ""` passed unmodified after both fixes, before any ingest work, the same discipline R1 established. Two required review amendments: a currents-step cron failure never blocks that pack's wind/wave fetch/hot-swap (`ingest/fetch_all_packs.py`); a >6h currents/weather time-coverage gap hard-errors rather than silently NaN-ing legs (≤6h holds nearest, logged). The A* heuristic's zero-current admissibility assumption is now a documented, deliberately-deferred limitation, not fixed. Med left off (real but small currents there; a one-line follow-up to enable). **Real UK acceptance run completed 2026-07-22** (initially paused pending Copernicus Marine Service credential registration): three departure times (0h/6h/12h) on the Plymouth↔Falmouth passage show a real, tide-attributable 9.5% duration/fuel penalty at t0=6h vs t0=0h, with t0=12h returning within 0.3% of t0=0h — matching the ~12.4h M2 semidiurnal cycle. External cross-check against a precise published numeric reference proved genuinely unavailable for free (UKHO EasyTide is height-only; Admiralty NP221/NP250 and POLPRED are paywalled/account-gated; CMEMS's own QUID validation document has no Channel/Celtic Sea current coverage) — recorded instead as two real, verifiable qualitative checks (semidiurnal periodicity, order-of-magnitude agreement with a secondary pilotage figure). Full design, live CMEMS research trace, and the real UK acceptance run: `docs/plans/ticket-C1.md`. | Ingest-layer, not new algorithmic work — the current-triangle math and weather-field plumbing already existed (A4/B2); this is real ingest + two real bugfixes found by finally exercising code paths that had never seen a nonzero current. |
| S1 — Route distillation | **Done (2026-07-21).** A bounded, provably-non-worsening post-search pass (`core/distill.py`) that simplifies each candidate's lattice/corridor polyline down to the deliberate waypoints a navigator would actually write down — quantisation kinks removed via the exact same `evaluate_leg` hard-constraint/cost machinery the search itself trusts, re-verified on every proposed shortcut (the Bonifacio scattered-islet lesson, ticket 0.8, applied at a different scale), never the search machinery itself. Search-machinery-untouched freeze defence (no lattice/turn-rate/heuristic/side-diversity change) — but unlike R1/C1, deliberately *not* a zero-behaviour-change ticket: plan-shape assertions (side, window feasibility, hard-constraint pruning, endpoint identity) hold unmodified by design; plan-numeric assertions were individually re-verified, including `test_pure_schedule_weights_converge_to_isochrone_time_optimal` (passed unmodified, as expected). `PlanRequest.distill: bool = True` (mirrored by `api/schemas.py`'s `PlanRequestIn.distill`) is the escape hatch, default on so every existing caller — hosted demo included — sees the improvement with zero client changes; a real `POST /v1/plans` with `distill=false` round-trips to a genuinely undistilled result. **One real, unexpected regression found and fixed, not the flagged test**: `optimise()`'s pre-existing top-3 `picks` selection was a greedy, cost-order-dependent algorithm that never actually guaranteed every reachable route side would be represented — it happened to work out that way before. S1's real, legitimate per-side cost improvements (uneven: the Bonifacio W corridor had more removable kinks than E) reordered the candidate pool enough to expose this, silently dropping Bonifacio's E-side candidate from the final picks (ticket 0.8's own regression test caught it). Fixed the algorithm itself, not the test (explicit user call): `picks` now guarantees one candidate per distinct reachable side before falling back to cost-order greedy fill, then re-sorts to restore presentation order (a second, smaller fix — the first alone broke `test_impossible_eta_window_flags_and_orders_fastest_first`'s fastest-first ordering). **Real acceptance run** (Med Antibes↔Porto Cervo + UK Plymouth↔Falmouth, real geography/weather): 50-81% fewer waypoints per candidate, no candidate ever scored worse, `distill_track` itself costs ~1.3-1.5% of total `optimise()` wall time — matching the B1 profiling gotcha's own ≤1% estimate. Full design, the picks-algorithm finding, and the real before/after numbers: `docs/plans/ticket-S1.md`. | Optimiser-adjacent, not a new optimiser feature — a bounded post-pass over already-chosen candidates, plus a targeted fix to a pre-existing candidate-selection gap S1's own improvements exposed. |
| L1 — Self-scaling lattice geometry | **Done (2026-07-22).** `core/lattice.py`'s lane spacing and turn allowance — previously Bonifacio (ticket 0.8)-tuned absolute constants inherited by every new pack via ticket R1's override mechanism, with no principled reason to be right for a different passage length — become derived from passage geometry (length-proportional lane spacing, angle-based turn allowance), each reducing to the Med's exact existing tuned constants by construction (verified bit-exact, real full-`optimise()` acceptance run, not just an isolated `build_lattice` check). Motivated by a real, empirically-diagnosed finding on the UK South-West pack (a 40% detour on the Plymouth↔Falmouth passage): `docs/plans/ticket-C1.md`'s dog-leg diagnostic. A clean sensitivity sweep ruled out both suspects a first look suggested — the C1-deferred A* heuristic admissibility gap (heuristic-guided and forced-exhaustive search returned byte-identical results) and `LANE_TURN_RATE_NM` (15→100nm made zero measured difference) — and isolated lane-spacing discretisation as the real mechanism (5nm lane grid far too coarse for a 36.7nm passage). Real acceptance result: detour ratio 1.398→1.104, a 21% score improvement, Med demo passage bit-exact/unaffected (`pytest -m ""` green, unmodified, 478 passed). A third planned piece, real-navigability-derived endpoint taper, was designed, implemented, and **cut** after a pre-agreed escape rule fired during implementation — it capped each stage's entire lane-index ceiling on single-point navigability, collapsing the lattice's deliberately wide search corridor far more broadly than "endpoint approach clearance" intended (12 real Med-suite test failures, not a narrow numeric drift); reported and reverted rather than debugged under this ticket, per the rule's own instruction. Shipped as lane-spacing + turn-allowance only. **Found and explicitly not fixed along the way**: a real NaN-cost silent-state-loss bug (`_lattice_search`'s Dijkstra/A* state update silently drops any edge whose cost comes back NaN — `nan < inf` is `False` in Python — with zero diagnostic, traced to a coarse weather grid's fully-masked interpolation stencil near a coastline) — elevated to an immediate fast-follow ticket, N1, since R1's arbitrary endpoints/favourites make this a live product-surface risk today (a user-placed endpoint near a coarse-grid coastal gap gets "no feasible route" with no explanation), not just a prerequisite for stage-spacing self-scaling. Full design, the empirical diagnosis (including the sensitivity-sweep data ruling out the wrong mechanisms), the escape-rule trace, and the real acceptance-run numbers: `docs/plans/ticket-L1.md`. | Search-machinery-untouched (S1's own freeze precedent, not R1/C1's zero-behaviour-change one) — a bounded change to what geometry the unmodified search is handed, not to the search itself; the escape rule firing mid-implementation is the discipline working as designed, not a partial delivery. |
| N1 — Non-finite leg cost becomes an explicit prune | **Done (2026-07-22).** L1's own fast-follow (§1c of that ticket), elevated at review from a someday item because R1's arbitrary endpoints/favourites are live product surface — a user-placed endpoint near a coarse weather grid's fully-masked stencil hit this today, silently, as an unexplained "no feasible route." `LegResult.non_finite_cost` (`core/legs.py`) is a sixth hard constraint, same shape as C1's `current_exceeds_stw`, checked via `math.isnan` specifically — not `isfinite` — since `duration_h` already legitimately becomes `+inf` under two pre-existing, already-correctly-handled codepaths, and flagging those too would have been a real, unwanted behaviour change, not a fix. **Verified the freeze-compatibility argument against the code rather than assuming it, per instruction — and it didn't hold uniformly**: true for `_lattice_search`/`core/isochrone.py` (a NaN-cost edge was already functionally pruned there, `nan < x` is always `False`, so the fix is genuinely diagnosability-only), **false for `_dp_route`** — its `if best is None or cost < best["cost"]:` accepted a NaN-cost edge unconditionally on the first try for a DP cell, and once accepted it could never be replaced (`finite < nan` is also `False`), a real, previously-shippable path to a `NaN`-scored `Candidate`, not just a silently-dropped option. A dedicated regression test (`tests/test_optimiser_constraints.py`) reproduces this via a funnel corridor forcing every downstream leg through the poisoned cell, and was verified to actually fail against a temporarily-reverted pre-N1 `_dp_route` before being trusted. A related finding while tracing the argument: `core.units.resolve_ground_speed_ms`'s own `remainder < 0` guard has the identical silent-bypass shape one level down (NaN current makes the guard meant to raise `ValueError` never fire) — but it lands on `+inf`, not `nan`, so it's safe today, just mislabelled (looks like "too slow," not "data gap") — named, not fixed. `PruneStats` (a lightweight shared counter, not hot-loop instrumentation) feeds a new `PruneDiagnostic(code="weather_data_gap")`, firing when a non-finite-cost prune occurred **and** either the candidate pool is empty **or** `missed_window` is true — the second leg a required review addition (a data gap pruning just the fast route while a slower one survives shows up as a window miss, not an empty pool). Lives in `core/legs.py`, not `core/optimiser.py` as first planned — a real mid-implementation deviation to avoid a circular import (`core/isochrone.py` already depends one-way on `core/legs.py`, never on `core/optimiser.py`, which itself imports *from* `core/isochrone.py`). Full design, the freeze-compatibility trace, and the real test-construction findings: `docs/plans/ticket-N1.md`. | Small, targeted — one new hard-constraint flag following an established pattern (C1's own), one real bug fix in `_dp_route`, one new diagnostic code; `pytest -m ""` green throughout, unmodified except for the new tests themselves. |
| L2 — Self-scaling stage spacing | **Done (2026-07-22).** L1's own named follow-up (`docs/plans/ticket-L1.md` §1c), unblocked by N1's non-finite-cost fix. Re-verified N1's safety claim empirically first: re-running L1's own along-track sensitivity sweep against post-N1 code, every previously-silent infeasibility (3.0/2.0/1.0nm) now carries a real, nonzero `non_finite_cost_count` (30/42/22) — never silent. **Headline finding, stated honestly**: a finer sweep than L1 ran found stage-spacing refinement has a **non-monotonic** relationship with detour ratio on the real Plymouth↔Falmouth passage (6 stages scores better than the shipped 7; 8-9 stages score worse; 4.5nm and finer infeasible outright) — unlike lane spacing's clean, monotonic L1 result, finer is not a lever that helps here. Confirmed a pure geometry effect, not a tide-timing artifact (identical detour-ratio sequence with currents zeroed): `build_lattice`'s stage centres sit at fixed along-track fractions, and the passage's two real obstructions (L1's own finding) sit at fixed fractions too — different stage counts land stage boundaries at different phases relative to those fixed obstructions, with no reason to be monotonic. `along_track_step_nm` now derives from passage length via the same Med-anchored-ratio principle as L1's `cross_track_step_nm`, but as a **floor** (`max`), not a ceiling — the reverse clamp direction, deliberately, since refining helps nowhere; guards only the opposite risk (a future much-longer-than-Med pack getting an excessive, needlessly expensive stage count under a fixed 6.0nm default). Verified a **deliberate, bit-exact no-op for both shipped packs**: Med floors to exactly 6.0 (as designed); UK's own unfloored formula output (~1.23nm) sits squarely in the found-infeasible zone, but the floor keeps it at exactly 6.0 too — real acceptance run confirms Med scores bit-exact-unchanged (13555.35/15721.78/17026.16, matching S1/L1's own numbers) and the UK detour ratio unchanged at 1.104. Full design, the real sweep tables, and the rounding-direction care (opposite of L1's own, flagged explicitly since it's easy to get backwards): `docs/plans/ticket-L2.md`. **Superseded finding, not this ticket's own code**: ticket W1 (below) found the non-monotonic/infeasible sweep above was an artefact of the UK weather grid's coastal coverage gap, not passage geometry — after W1's fill, every value is feasible and finer measurably helps (3.0nm beats the shipped 6.0nm default). This ticket's own floor-clamp formula is unchanged and still correct for its coarsening-direction purpose; see `docs/plans/ticket-L2.md`'s own "Follow-up (ticket W1)" section for the full re-evaluation. | Small — one formula, one clamp-direction reversal from L1's own precedent, honest about not moving the headline metric it was named to chase; `pytest -m ""` green throughout, unmodified except for the new derivation-formula unit tests. |
| W1 — Coastal weather coverage | **Done (2026-07-22).** Motivated by L2's own sweep hitting real `non_finite_cost` prunes on the UK pack's coarse weather grid. **Part 1 empirically tested, and refuted, a third motivating hypothesis**: instrumenting a real `distill_track` run found zero of 34 `evaluate_leg` calls returned `non_finite_cost=True` — the residual Plymouth-approach kink S1 leaves in place is blocked by genuine `navigable=False`/`depth_ok=False` (real coastline/shoal water), not a weather gap; W1 does not smooth that kink. **A larger, unplanned finding reshaped the design**: checking the real UK pack's masked cells directly found only 14 of 31 are actually land at their own GSHHG reference point — the other 17 are NaN despite reading as open water, a real characteristic of NOAA WW3's own internal nearshore land-sea mask (a source-model limitation, not an ingest bug), not the land-mislabelling the ticket's own framing assumed. Broadened the fill trigger accordingly, signed off explicitly: fill any currently-NaN cell whose footprint is majority real navigable water, regardless of why it went NaN — mechanically identical work, but covers the real majority case instead of missing it. Two new shared functions in `ingest/grib_common.py` (`coastal_fill_mask`/`apply_coastal_fill`, geometry computed once per fetcher run, reused across every field in a group) wired into all four fetchers (`fetch_grib_nomads.py`, `fetch_grib_ecmwf.py`, `fetch_era5_track.py`, `fetch_currents_cmems.py`/`merge_currents.py`). Two judgment calls signed off with real supporting numbers: `water_fraction_threshold=0.5` (majority water, not "any intersection" — the naive reading would have filled 27/31 masked cells including ones as low as 7% water, overwhelmingly inland Dartmoor with a creek in one corner; 0.5 drops this to 18, all genuinely majority-water); `max_fill_radius_nm=60.0` (covers all 18 real UK cells, nearest-neighbour distances up to 53.4nm at the sparsest corners, while still a real bound, not unconditional fill — a WARNING logs for any individual fill past 30nm, making long-range fills visible in cron logs without lowering the 60nm ceiling). Real UK re-ingest: `wave_filled_cells` and real before/after masked-cell counts recorded in `docs/plans/ticket-W1.md`. `GriddedWeatherField` gained two optional provenance fields (`wave_filled_cells`/`current_filled_cells`, the same tolerant-load pattern C1's `current_*` triple established), surfaced via `GET /v1/health`. Zero `core/lattice.py`/`core/optimiser.py`/`core/legs.py`/`core/isochrone.py`/`core/corridors.py` diff — confirmed mechanically. **Real UK re-ingest (2026-07-22, live NOMADS run) confirmed the planning-pass numbers exactly**: `wave_filled_cells=18`, the Plymouth-Sound-adjacent cell now holds a real value where it was `NaN` before. **Re-running L2's own §1b sweep against the refilled grid found every previously-infeasible value now feasible** (`non_finite_cost_count=0` throughout) and, unlike L2's own pre-W1 finding, **finer stage spacing now measurably helps** (3.0nm scores best of the values tested — see `docs/plans/ticket-L2.md`'s new "Follow-up (ticket W1)" section, added rather than silently amending L2's shipped conclusion). **The real, headline, product-visible number**: the coastal fill alone (still at the shipped, L2-floored 6.0nm stage spacing — no search/lattice code changed) moves the real `optimise()` Plymouth↔Falmouth detour ratio from **1.104 to 1.0412**, score 3487.62→3378.85. The S1 distillation trace re-run unchanged (still zero `non_finite_cost` calls, still blocked by genuine coastline/depth, not weather) — Part 1's refutation holds. Full design, both empirical findings, and the real fill/acceptance numbers: `docs/plans/ticket-W1.md`. | Ingest-layer only, by design and verified — ingests four fetchers already share one masking helper; the fill is a second pass through the same helper's geometry, no new search/cost/hard-constraint code. |
| R2-lite — Region pack expansion (data work, no new engineering) | **Done (2026-07-22).** Two new real region packs generated end-to-end via `docs/region-pack-runbook.md`, exactly as written, zero `core/` diff: `med_full` (Gibraltar to the Aegean, 544 deg², deliberately superseting but not touching/retiring the existing `med` pack) and `caribbean` (Windwards through the Virgin Islands + Bahamas, 328 deg²). **One real, blocking bug found and fixed**: both first-attempt GEBCO fetches failed with `fsspec.exceptions.FSTimeoutError` after ~14 minutes — `fetch_subset`'s `.values` call pulls a whole large bbox slice in one long-lived HTTP read, and fsspec's default client had no timeout generous enough to distinguish "still transferring" from "stalled." Fixed with a minimal, explicit `client_kwargs={"timeout": aiohttp.ClientTimeout(total=3600.0)}` (`ingest/fetch_gebco.py`); both fetches then succeeded (51.5MB/31.0MB bathymetry). **A real, two-layer endpoint-navigability finding** on `med_full`'s originally-proposed Palma→Porto Cervo passage: WPI's raw Palma pin sat on rasterised land (expected, `uk_sw.yaml`'s own precedent); a first small nudge was individually navigable but still made `optimise()` raise "baseline infeasible" — traced to only 1 of 33 first-stage lattice legs from that point having genuinely clear water (the Bonifacio scattered-islet lesson recurring: point navigability isn't leg navigability), fixed by nudging further into open Palma Bay (leg-fan-verified before committing). A third, deliberately-not-fixed finding surfaced immediately after: `core/optimiser.py`'s existing, unmodified fixed-14kn/2-engine `_baseline_route` reference couldn't complete a continuous single-speed passage through a genuine destination-side chokepoint near Sardinia, even though the real multi-speed candidate search succeeded — real, pre-existing machinery finally exercised by a long/constrained-enough passage, not a new bug; named as a follow-up for whichever ticket tackles longer cross-basin passages (closer to R3's "ocean-crossing" scope than R2's). Resolved pragmatically: `med_full`'s default passage reuses the existing `med` pack's already-proven Antibes↔Porto Cervo pair instead (real, independently verified through `med_full`'s own much larger geography/weather data: score 13505.24, detour ratio 1.0501, reproducing the exact known Bonifacio-stage refinement behaviour end-to-end). `caribbean`'s St Thomas↔St Barths endpoints needed no such second layer, validated cleanly first try (detour ratio 1.0000, score 8600.10). **W1's coastal fill engaged automatically on both new, much-larger real grids with zero code changes** (`wave_filled_cells` 441/293). **Self-scaling lattice formulas (L1/L2) proven unaided at two more real scales**, no yaml overrides on either pack: `caribbean`'s 121.59nm passage derives `cross_track_step_nm=3.386` (between the UK pack's 0.5-ceiling and the Med's 5.0-ceiling) and floors `along_track_step_nm` to 6.0, exactly as designed. **A real, measured performance finding**: the identical Antibes↔Porto Cervo passage costs 24.4s through `med_full` vs. ~6s through the small `med` pack — plausibly `RealGeography.is_land_precise`/`is_navigable`'s per-call linear scan over every loaded GSHHG polygon (3,340 for `med_full` vs. far fewer for the small bbox), not re-profiled further (would be new engineering) but named for whoever next needs a large-pack performance pass. VM/cron implications: recurring weather-refresh bandwidth for all 4 packs combined ≈ 14.6MB/cycle (from ≈84KB today, still trivial); one-time bathymetry disk for the two new packs ≈ 82.5MB. Full execution log, every real number, and the runbook friction found: `docs/plans/ticket-R2-lite.md`. `pytest -m ""` (503 tests) green, unmodified except for the new packs' own manifests/data; `ruff check .` clean; zero `core/` diff confirmed mechanically. | Data work via existing tooling, as scoped — but surfaced one real ingest-layer bug fix (GEBCO timeout) and two real, honestly-named findings about existing `core/optimiser.py` machinery meeting a longer/harder passage for the first time, neither fixed here per the ticket's own no-new-engineering boundary. |
| R2 — Region pack pipeline (Phase 2) | Cloud-side pack generation (full Med, Caribbean/Bahamas, US East Coast, N. Europe, Red Sea/Gulf, SE Asia), synced to vessels ahead of need — the chart-folio model mariners already know. | Engineering + storage discipline (GEBCO fits on the edge box; masks tile to ~100s MB/region). |
| R3 — Ocean passages | Proper spherical geometry (equirectangular + single REF_LAT break beyond ~500nm; lattices follow great circles); receding-horizon planning (forecast skill ~10 days < transat duration → blend climatology/pilot-chart statistics beyond the horizon); currents as a genuinely *global* field, ocean-crossing (Gulf Stream routing) — C1 (done) already covers the *regional* case, this is the remaining ocean-scale/spherical-geometry piece; tropical-cyclone forecast cones as time-dependent hard-constraint polygons. | The real algorithmic step. |
| R4 — Global topology | Two-level planner: coarse precomputed global sea graph decides strategy (straits, Suez vs Cape, canals as scheduled/fee-bearing edges); the existing regional lattice refines each section unchanged. Global TSS/ECA/MPA/security-area data. | Hierarchical routing is standard; the heavy lift is data licensing/curation, not code. |

Nothing in the current architecture fights this: edge-first survives (packs sync ahead, planning stays offline), the twin is geography-agnostic, the time-expanded search generalises. Only the geometry layer and corridor assumptions are Med-shaped today.

## Deferred (post-prototype, pre-product)

**Gribbosaurus Rex integration (separate project, same box):** multi-model GRIB confidence scoring against weighted live observations — the missing arbiter for the ingest layer's already-multi-source design, and the shore-side half of spec differentiator 4.3's correction network. v1 integration is deliberately thin: Gribbosaurus publishes a scores artefact (model/region/lead-time/score/obs-count); the cloud role's existing cron consults it and copies the winning model's npz to the served path — existing hot-swap and provenance do the rest, zero `core/` or vessel-side changes, and voyage debriefs automatically record which model each plan ran on. Later: per-lead-time blending, feeding onboard correction, pilot vessels as observation sources. Cohosted on the cloud VM (own user/service/subdomain; shares system eccodes; watch GRIB disk retention). IP note: bring under the company umbrella before the CMDC bid freezes partner/IP descriptions.

Custom edge hardware rev B (IEC 60945 EMC), vendor monitoring-system adapters (Böning/Praxis), fleet weather-correction network, fleet dashboard for managers, ENC licensing (S-57/S-63 via PRIMAR/UKHO), flag/class written comfort (start conversations earlier, per spec §11.4), anchor/hotel-load optimisation.

## Top risks

1. **Twin accuracy on thin data** (1.5) — mitigate with sister-ship priors, flowmeter upgrade on pilot vessel if needed.
2. **Bus-data chaos** (1.3) — mitigate with survey checklist and generous commissioning budget.
3. **Pilot vessel availability** (1.1) — recruit two, assume one.
4. **Scope creep toward product** — the sea trial needs one vessel and one passage type; resist fleet features until 2.7 is done.
